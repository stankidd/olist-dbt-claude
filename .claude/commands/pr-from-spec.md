# /pr-from-spec

Create a new git branch using naming convention: initials/use-case-name. Commit all changes. Push branch. Open a PR using the PR template in templates/. Include: requirements answered, models built, tests run, checklist complete.
